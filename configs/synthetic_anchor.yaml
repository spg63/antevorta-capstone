# W4-01 — permanent synthetic e2e anchor (spec §11).
# Full infer → arena → aggregate loop on separable data; ~100% accuracy expected.
name: synthetic_anchor
kind: synthetic_anchor
seed: 42
n_runs: 10
params:
  crowd_config: configs/crowd_smoke.yaml
  n_train: 400
  n_test: 200
